import { Component } from '@angular/core';

@Component({
  selector: 'app-registry',
  standalone: true,
  template: `
    <section aria-label="Records Registry">
      <h1>Records Registry</h1>
      <p>Record registration and management interface.</p>
    </section>
  `
})
export class RegistryComponent {}
