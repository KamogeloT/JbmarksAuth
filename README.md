# JBmarks Token Exchange Server

Simple Node.js/Express server for OAuth token exchange with Bitrix24.

## Deploy to Railway

This project is configured for Railway deployment with:
- Node.js 18+
- Express.js
- Simple HTTP endpoints

## Endpoints

- `GET /health` - Health check
- `POST /api/exchangetoken` - Token exchange
