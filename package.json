{
  "name": "jbmarks-root",
  "version": "1.0.0",
  "description": "Root package for JBmarks project - BFF API is in azure-bff-api folder",
  "scripts": {
    "start": "cd azure-bff-api && node server.js",
    "build": "cd azure-bff-api && npm install",
    "postinstall": "cd azure-bff-api && npm install"
  },
  "engines": {
    "node": ">=18"
  }
}